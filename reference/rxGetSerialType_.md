# Get the serialization type from raw vector

Get the serialization type from raw vector

## Usage

``` r
rxGetSerialType_(raw)
```

## Arguments

- raw:

  A raw vector

## Value

a string indicating the serialization type: "qs2", "qdata", "qs",
"base", or "unknown"
