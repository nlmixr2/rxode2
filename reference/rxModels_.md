# Get the rxModels information

Get the rxModels information

## Usage

``` r
rxModels_(env = TRUE)
```

## Arguments

- env:

  boolean that returns the environment where models are stored (TRUE),
  or the currently assigned rxode2 model variables (FALSE).

## Value

internal rxModels information environment
