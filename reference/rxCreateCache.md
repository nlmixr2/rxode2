# This will create the cache directory for rxode2 to save between sessions

When run, if the `R_user_dir` for rxode2's cache isn't present, create
the cache

## Usage

``` r
rxCreateCache()
```

## Value

nothing

## Author

Matthew Fidler
