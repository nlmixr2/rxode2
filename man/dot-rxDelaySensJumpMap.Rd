% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dde.R
\name{.rxDelaySensJumpMap}
\alias{.rxDelaySensJumpMap}
\alias{.rxDelaySensJumpEvents}
\alias{.rxDelaySensJump}
\alias{.rxDelaySensJumpMapCached}
\title{Dose-induced breaking-point jump for forward delay sensitivities}
\usage{
.rxDelaySensJumpMap(model, calcSens)

.rxDelaySensJumpEvents(jumpMap, st, events)

.rxDelaySensJump(model, calcSens, events)

.rxDelaySensJumpMapCached(model, calcSens, keyTxt = NULL)
}
\arguments{
\item{model}{base ODE model (anything \code{rxNorm()} accepts).}

\item{calcSens}{character vector of sensitivity parameters.}

\item{jumpMap}{the jump map from \code{.rxDelaySensJumpMap()} (\verb{$jumpMap}).}

\item{st}{ODE state names (\verb{$st} from \code{.rxDelaySensJumpMap()}), for numeric-cmt
resolution.}

\item{events}{an rxode2 event table (to mirror the state-j doses onto the
sensitivity compartments).}

\item{keyTxt}{normalized model text used as the cache key (pass \code{rxNorm(model)}
if already computed, else it is derived).}
}
\value{
\code{.rxDelaySensJumpMap()}: list with \code{alagf}, \code{jumpMap}, and \code{st}, or
\code{NULL} when the model has no parameter-dependent delay.  \code{.rxDelaySensJump()}:
list with \code{alagf} and \code{events}, or \code{NULL}.
}
\description{
A dose is a state discontinuity; propagated through \code{delay(y_j, T(p))} it makes
the delayed value jump at \code{t = t_dose + T(p)}, so the 1st-order sensitivity
\code{S_i^p} jumps there by \verb{[S_i] = -(d f_i/d delay(y_j,T)) * [y_j] * dT/dp} -- a
Dirac the smooth \code{rxDelayD} term in \code{\link[=.rxDelaySensAugment]{.rxDelaySensAugment()}} misses.  Rather than
insert runtime break events, this reproduces the jump with an ordinary modeled
bolus on the sensitivity compartment: a dose of \verb{[y_j]} with modeled lag \code{T}
(so it lands at \code{t_dose + T}) and modeled bioavailability \verb{-(d f_i/d delay)*dT/dp}
(so the delivered amount is exactly \verb{[S_i]}).
}
\details{
\code{.rxDelaySensJumpMap()} does the model-only (symengine) analysis: it returns
the \code{alag()}/\code{f()} model lines and the jump map (which sensitivity compartment
mirrors which dosed base state).  This part depends only on the model, so it is
cached (see \code{.rxDelaySensJumpMapCached()}).  \code{.rxDelaySensJumpEvents()} is the
cheap per-solve step that rbinds the mirroring doses onto the event table.
\code{.rxDelaySensJump()} is the convenience wrapper doing both.
}
\author{
Matthew L. Fidler
}
\keyword{internal}
