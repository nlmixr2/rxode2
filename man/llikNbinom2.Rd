% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llik.R
\name{llikNbinom2}
\alias{llikNbinom2}
\title{Calculate the log liklihood of the negative binomial function (and its derivatives)}
\usage{
llikNbinom2(x, size, mu, full = FALSE)
}
\arguments{
\item{x}{Number of successes}

\item{size}{Size of trial}

\item{mu}{mu parameter for negative binomial}

\item{full}{Add the data frame showing x, mean, sd as well as the
fx and derivatives}
}
\value{
data frame with \code{fx} for the pdf value of with
\code{dProb} that has the derivatives with respect to the parameters at
the observation time-point
}
\description{
Calculate the log liklihood of the negative binomial function (and its derivatives)
}
\details{
In an \code{rxode2()} model, you can use \code{llikNbinom2()} but you have to
use all arguments.  You can also get the derivative of \code{mu} with
\code{llikNbinom2Dmu()}
}
\examples{

llikNbinom2(46:54, 100, 40)

llikNbinom2(46:54, 100, 40, TRUE)

et <- et(46:54)
et$size <- 100
et$mu <- 40

model <- rxode2({
  fx <- llikNbinom2(time, size, mu)
  dProb <- llikNbinom2Dmu(time, size, mu)
})

rxSolve(model, et)
}
\author{
Matthew L. Fidler
}
