% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert.R
\name{rxUiPriors}
\alias{rxUiPriors}
\title{Priors specified in a model}
\usage{
rxUiPriors(ui)
}
\arguments{
\item{ui}{rxode2 ui model}
}
\value{
data frame of the parameters that carry a prior, with the
columns \code{name}, \code{prior} (the prior as written, ie \code{"invWishart(4)"}),
\code{neta1}/\code{neta2} (\code{NA} for a population parameter) and \code{lower}/\code{upper}
from the parameter, which is what gives a truncated prior its bounds.
Zero rows when the model has no priors, and also when the \code{iniDf} has
no \code{prior} column at all.
}
\description{
This is the accessor an estimation method uses to \emph{implement} priors,
as opposed to the \verb{assertRxUi*} functions which reject the ones it
cannot implement.
}
\examples{

\donttest{
one.cmt <- function() {
 ini({
   tka <- 0.45; label("Ka")
   tcl <- log(c(0, 2.7, 100)); label("Cl")
   tv <- 3.45; label("V")
   eta.ka ~ 0.6
   eta.cl ~ 0.3
   eta.v ~ 0.1
   add.sd <- 0.7
 })
 model({
   ka <- exp(tka + eta.ka)
   cl <- exp(tcl + eta.cl)
   v <- exp(tv + eta.v)
   linCmt() ~ add(add.sd)
 })
}

# a model with no priors gives a zero row data frame; a prior is added
# with `prior(tka) ~ dnorm(0, 10)` in the `ini({})` block, which needs a
# 'lotri' new enough to support them
rxUiPriors(one.cmt)
}
}
\seealso{
Other Assertions:
\code{\link[=assertCompartmentExists]{assertCompartmentExists()}},
\code{\link[=assertCompartmentName]{assertCompartmentName()}},
\code{\link[=assertCompartmentNew]{assertCompartmentNew()}},
\code{\link[=assertRxUi]{assertRxUi()}},
\code{\link[=assertVariableExists]{assertVariableExists()}},
\code{\link[=assertVariableNew]{assertVariableNew()}},
\code{\link[=rxPriorBuildSpec]{rxPriorBuildSpec()}},
\code{\link[=rxPriorLogDensity]{rxPriorLogDensity()}},
\code{\link[=testIniDf]{testIniDf()}},
\code{\link[=testRxUnbounded]{testRxUnbounded()}}
}
\author{
Matthew L. Fidler
}
\concept{Assertions}
