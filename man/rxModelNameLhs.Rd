% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui-modelName.R
\name{rxModelNameLhs}
\alias{rxModelNameLhs}
\title{Name a model from the left hand side of an assignment}
\usage{
rxModelNameLhs(value)
}
\arguments{
\item{value}{when specified, a single non-empty character naming the model,
or \code{NULL} to clear the name.  When missing the current name is returned.}
}
\value{
the registered name, or \code{NULL} when there is none
}
\description{
Registers the name an assignment operator like \code{nlmixr2save}'s \verb{:=} is
assigning to, which rxode2 uses when the model expression itself names
nothing (an anonymous model function) and when the expression is a call
without a \code{rxModelName()} method.  It is not consumed when it is used, so
one assignment can name every model it builds; the operator that set it
clears it with \code{rxModelNameLhs(NULL)}, which is what keeps the name from
leaking into an unrelated later model.
}
\examples{

# an assignment operator registers the name it is assigning to before it
# forces the model, and clears it afterward:
#
# `:=` <- function(x, value) {
#   rxode2::rxModelNameLhs(as.character(substitute(x)))
#   on.exit(rxode2::rxModelNameLhs(NULL))
#   assign(as.character(substitute(x)), force(value), envir=parent.frame())
# }

rxModelNameLhs()

rxModelNameLhs("mod")

rxModelNameLhs()

rxModelNameLhs(NULL)

}
\seealso{
Other Model names:
\code{\link[=rxModelName]{rxModelName()}},
\code{\link[=rxModelNameFromExpr]{rxModelNameFromExpr()}}
}
\author{
Matthew L. Fidler
}
\concept{Model names}
