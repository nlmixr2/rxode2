% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxIndLin.R
\name{rxIndLinExpStats}
\alias{rxIndLinExpStats}
\title{Matrix-exponential cache statistics for inductive linearization}
\usage{
rxIndLinExpStats(reset = FALSE)
}
\arguments{
\item{reset}{logical; when \code{TRUE} zero the counters after reading, so a
measurement is one call before the work and one after}
}
\value{
named numeric vector: \code{computed} (exponentials actually
exponentiated), \code{reused} (exponentials served from a cache slot), \code{noSlot}
(the subset of \code{computed} that ran on a thread with no cache slot of its
own, so it could not have hit -- nonzero means the pool was never sized or
is smaller than the thread team), and \code{slots} (the current pool size)
}
\description{
Reports whether the per-thread content-addressed exponential cache used by
\code{matExp()}/\code{indLin()} solving actually served anything.  The same numbers ride
out of an \code{\link[=rxSolve]{rxSolve()}} as \verb{$counts$dadt} (computed) and \verb{$counts$jac} (reused),
but a fit drives the solver directly and never builds that data frame, so this
is the only way to observe the cache from inside one.
}
\examples{
rxIndLinExpStats()
}
\author{
Matthew L. Fidler
}
\keyword{internal}
