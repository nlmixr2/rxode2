% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxsolve.R
\name{rxRegisterUiAssembled}
\alias{rxRegisterUiAssembled}
\alias{rxRemoveUiAssembled}
\title{Register or remove a ui assembly hook}
\usage{
rxRegisterUiAssembled(name, fn)

rxRemoveUiAssembled(name)
}
\arguments{
\item{name}{Unique hook name; re-registering the same name replaces it.}

\item{fn}{Function of one argument (the freshly assembled ui environment).
The return value is ignored; errors are downgraded to a warning so a buggy
hook cannot break unrelated model builds.}
}
\value{
Invisibly, the hook name (register) or \code{NULL} (remove).
}
\description{
Package developers register a function called with a freshly assembled \code{rxUi}
before it is compressed.  Use it to attach parse-time state to the model --
for example values that a user-defined function (see \code{\link[=rxUdfUi]{rxUdfUi()}}) generated
while the model was being parsed, which the \code{rxUdfUi()} return list has no
field to carry.
}
\details{
The hook receives the ui \strong{environment} and may assign into it in place;
pair that with the \code{sticky} mechanism (see \code{\link[=rxForcedPars]{rxForcedPars()}}) so the slot
survives model piping and \code{saveRDS()}.  A hook must not reorder
\code{rxModelVars(ui)$params}, must be cheap, and must be a no-op for models it
does not own.
}
\author{
Matthew L. Fidler
}
