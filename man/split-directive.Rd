% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splitBolus.R
\name{split-directive}
\alias{split-directive}
\alias{split}
\alias{split()}
\title{Split doses across compartments, bolus or infusion (model directive)}
\description{
\code{split()} is a model-only directive that rewrites doses aimed at
\code{cmt} into parallel doses to the target compartments, regardless of
whether the dose record is a bolus or an infusion. Each target
compartment receives the full original amount; use \code{f()} to scale
the split.

A plain bolus record targeting a compartment that declares a modeled
\code{dur()} or \code{rate()} property is promoted to a modeled infusion
start/stop pair for that compartment, so a single bolus dose record
can feed both an infusion path and a bolus path (Monolix-style
double absorption with mixed zero- and first-order routes).
Steady-state bolus records are not promoted; they are copied as
boluses with a warning when a target declares \code{dur()}/\code{rate()}.

Infusion records (data \code{RATE}/\code{DUR} or modeled \code{rate()}/\code{dur()}) are
split preserving their type, exactly like \code{\link[=splitInfusion]{splitInfusion()}}.

This rewrite applies at \code{\link[=etTrans]{etTrans()}} translation time only; doses
pushed while solving with \code{\link[=evid_]{evid_()}} are not split. Only one
splitting directive (\code{splitBolus()}, \code{splitInfusion()} or \code{split()})
is allowed per model.

There is no R function named \code{split()} exported by rxode2 (it would
mask \code{\link[base:split]{base::split()}}); the directive is parsed from the model text
directly, as shown below.
}
\section{Usage inside a model}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{model(\{
  split(depot, central, depot2)
  dur(central) <- tk0
  f(central) <- f1
  f(depot2) <- 1 - f1
  d/dt(depot2) <- -ka2 * depot2
  d/dt(central) <- ka2 * depot2 - cl / v * central
  ...
\})
}\if{html}{\out{</div>}}

A bolus dose recorded against \code{depot} is split so \code{central} receives
a modeled-duration infusion (zero-order input over \code{tk0}, scaled by
\code{f1}) and \code{depot2} receives the bolus (first-order input scaled by
\code{1 - f1}).
}

