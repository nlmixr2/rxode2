% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxsolve.R
\name{rxIsNonStiff}
\alias{rxIsNonStiff}
\title{Check whether an ODE solving method is a purely non-stiff solver}
\usage{
rxIsNonStiff(method)
}
\arguments{
\item{method}{A character vector of method names or an integerish vector of
method codes (as returned by \code{\link[=odeMethodToInt]{odeMethodToInt()}}).  Vectorised.}
}
\value{
A logical vector the same length as \code{method}.  \code{TRUE} if the
corresponding method is a purely non-stiff solver.
}
\description{
Returns \code{TRUE} for methods that are designed exclusively for non-stiff
systems.  Solvers like \code{"lsoda"} and \code{"liblsoda"} that automatically switch
between stiff and non-stiff algorithms return \code{FALSE}, as do all stiff-only
methods.
}
\details{
Switchers (\code{"lsoda"} = 1, \code{"liblsoda"} = 2) and the inductive linearisation
solver (\code{"indLin"} = 3) are excluded from both \code{\link[=rxIsStiff]{rxIsStiff()}} and
\code{rxIsNonStiff()}.
}
\examples{
rxIsNonStiff("dop853")                        # TRUE
rxIsNonStiff("lsoda")                         # FALSE (switches)
rxIsNonStiff("bdf")                           # FALSE (stiff-only)
rxIsNonStiff(c("lsode", "cvode", "bs32"))     # TRUE FALSE TRUE

}
\seealso{
\code{\link[=rxIsStiff]{rxIsStiff()}}, \code{\link[=rxIsImplicit]{rxIsImplicit()}}, \code{\link[=odeMethodToInt]{odeMethodToInt()}}
}
