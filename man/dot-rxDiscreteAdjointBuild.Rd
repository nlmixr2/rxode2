% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjointDiscrete.R
\name{.rxDiscreteAdjointBuild}
\alias{.rxDiscreteAdjointBuild}
\title{Build symbolic evaluators for the discrete adjoint (RHS, Jacobian, forcing)}
\usage{
.rxDiscreteAdjointBuild(object, calcSens)
}
\arguments{
\item{object}{model definition accepted by \code{\link[=rxode2]{rxode2()}}.}

\item{calcSens}{character vector of parameters to differentiate w.r.t.}
}
\value{
a list with \code{st} (state names), \code{ns}, \code{np}, \code{calcSens}, and evaluator
closures \code{Fe(x, params)}, \code{FXe(x, params)} (\verb{ns x ns}), \code{FPe(x, params)}
(\verb{ns x np}).
}
\description{
Parses the model once and returns closures that evaluate the ODE RHS \code{F}, the
state Jacobian \code{dF/dy} and the parameter forcing \code{dF/dtheta} at an arbitrary
state (needed at interior RK4 stage points).  Pair with
\code{\link[=.rxDiscreteForwardSens]{.rxDiscreteForwardSens()}} and \code{\link[=.rxDiscreteAdjointGrad]{.rxDiscreteAdjointGrad()}}.
}
\author{
Matthew L. Fidler
}
\keyword{internal}
