% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splitBolus.R
\name{splitInfusion}
\alias{splitInfusion}
\title{Split infusion doses across compartments inside an rxode2 model}
\usage{
splitInfusion(cmt, ...)
}
\arguments{
\item{cmt}{Source compartment name.}

\item{...}{Target compartment names. Provide at least one target
compartment.}
}
\value{
This function is only meaningful inside an rxode2 model; it
errors when called directly from R.
}
\description{
\code{splitInfusion()} is a model-only directive that rewrites infusion
doses aimed at \code{cmt} into parallel infusion doses to the target
compartments.  Each target compartment receives the full original
amount (or rate); use compartment properties like \code{f()} to scale the
split (for example a Monolix-style double-absorption \code{F1}/\code{1-F1}
dose apportionment).  The source-compartment infusion is replaced,
not retained.

Both data-driven infusions (\code{RATE} or \code{DUR} records) and modeled
\code{rate()}/\code{dur()} infusions are split.  The source and every start
or stop record of the infusion is re-targeted, so the pairings the
solver relies on are preserved.

Unlike \code{splitBolus()}, this rewrite applies at \code{\link[=etTrans]{etTrans()}}
translation time only; doses pushed while solving with \code{\link[=evid_]{evid_()}}
are not split.

The source compartment may also appear in the target list, but the
target compartments in \code{...} must be unique.
}
