% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splitBolus.R
\name{splitBolus}
\alias{splitBolus}
\title{Split bolus doses across compartments inside an rxode2 model}
\usage{
splitBolus(cmt, ...)
}
\arguments{
\item{cmt}{Source compartment name.}

\item{...}{Target compartment names. Provide at least two target
compartments.}
}
\value{
This function is only meaningful inside an rxode2 model; it
errors when called directly from R.
}
\description{
\code{splitBolus()} is a model-only directive that rewrites bolus doses
aimed at \code{cmt} into parallel bolus doses to the target compartments.
Each target compartment receives the full original amount. The
source-compartment bolus is replaced, not retained.

This rewrite applies both to event tables translated by \code{\link[=etTrans]{etTrans()}}
and to future doses scheduled during solving with \code{\link[=evid_]{evid_()}}.

The source compartment may also appear in the target list, but the
target compartments in \code{...} must be unique.
}
