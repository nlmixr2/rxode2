% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etaDist.R
\name{rxEtaDistExpand}
\alias{rxEtaDistExpand}
\title{Expand declared non-normal random effect distributions into a model}
\usage{
rxEtaDistExpand(ui)
}
\arguments{
\item{ui}{rxode2 ui}
}
\value{
the rewritten rxode2 ui, or \code{ui} itself when there is nothing
to expand
}
\description{
Rewrites a ui that carries \code{dist()} declarations into an ordinary ui:
the declared random effects become latent standard normals (a fixed
identity omega) plus unconstrained correlation thetas, and the model
block gains the \code{phiU()} + inverse CDF lines that recreate them under
their original names.  Everything downstream -- \code{rxSolve()}, and every
nlmixr2est estimation method -- then sees a model it already knows how
to handle.
}
\details{
A ui with no declaration is returned unchanged.
}
\examples{

\donttest{
one.cmt <- function() {
  ini({
    lclm <- log(5)
    lclrv <- log(0.09)
    tv <- 3.45
    eta.v ~ 0.1
    eta.cl ~ 1
    dist(eta.cl) ~ dgamma(shape=1/exp(lclrv),
                          rate=1/(exp(lclrv)*exp(lclm)))
    add.sd <- 0.7
  })
  model({
    cl <- eta.cl
    v <- exp(tv + eta.v)
    linCmt() ~ add(add.sd)
  })
}

rxEtaDistExpand(one.cmt())
}
}
\author{
Matthew L. Fidler
}
