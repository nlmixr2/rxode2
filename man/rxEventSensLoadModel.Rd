% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eventSens.R
\name{rxEventSensLoadModel}
\alias{rxEventSensLoadModel}
\title{Point the rxode2 event-sensitivity globals at a jump-sensitivity model}
\usage{
rxEventSensLoadModel(model)
}
\arguments{
\item{model}{A built jump-sensitivity model (carrying \code{eventSensInfo}).}
}
\value{
invisibly \code{TRUE} when the jumps were activated, \code{FALSE} otherwise
(fd model / no sensitivities).
}
\description{
For downstream packages (e.g. nlmixr2est's FOCEi) that solve a sensitivity
model through a direct C++ \code{ind_solve()} loop, bypassing \code{rxSolve()}: sets
rxode2's event ("jump") sensitivity function pointers and runtime dims to
\code{model} and turns the jumps on.  The jump blocks are bounds-guarded, so
smaller models solved afterwards skip the injection safely.  Pair with
\code{rxEventSensDeactivate()} after the run.
}
\keyword{internal}
