% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etaDist.R
\name{.rxEtaDistDerivLines}
\alias{.rxEtaDistDerivLines}
\title{Derivative anchors for a declaration's arguments}
\usage{
.rxEtaDistDerivLines(anc, thetas)
}
\arguments{
\item{anc}{the result of \code{.rxEtaDistAnchors()}, whose \code{"lines"} attribute
holds the \verb{rxEdA.<eta>.<role> <- <expr>} assignments}

\item{thetas}{character vector of declared theta names}
}
\value{
character vector of assignments, possibly empty
}
\description{
For each argument anchor \verb{rxEdA.<eta>.<role>} and each declared theta the
expression mentions, emit
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{rxEdD.<eta>.<role>.<theta> <- d(<arg expression>)/d(<theta>)
}\if{html}{\out{</div>}}

so the gradient reads an exact derivative off the solve instead of central
differencing the argument expression per record per theta.  The arguments
are already model lines (see \code{.rxEtaDistAnchors()}); their derivatives are
the same trick one step further, and they inherit the same covariate
handling -- a covariate in the expression is a covariate in its derivative.

STRUCTURAL ZEROS ARE DROPPED.  A gamma's shape does not involve the mean's
theta, so \code{d(shape)/d(lclm)} is exactly 0; emitting a line for it would have
the model compute a constant zero per observation.  \code{rxJacobian} drops them
the same way (\verb{if (paste(.d) != "0")}).  The consumer must therefore treat a
MISSING derivative anchor as a true zero rather than as a failure.
}
\author{
Matthew L. Fidler
}
\keyword{internal}
