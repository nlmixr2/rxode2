% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorDensity.R
\name{rxPriorOmegaToCholOmegaInvGrad}
\alias{rxPriorOmegaToCholOmegaInvGrad}
\title{Chain-rule a natural-scale omega gradient into FOCEI's cholOmegaInv scale}
\usage{
rxPriorOmegaToCholOmegaInvGrad(omega, gradOmega)
}
\arguments{
\item{omega}{a single omega block (p x p numeric matrix, positive
definite)}

\item{gradOmega}{the corresponding block of \code{rxPriorLogDensity()}'s
\code{gradOmega}, i.e. \code{gradOmega[block, block]} (p x p, symmetric)}
}
\value{
p x p matrix, the gradient with respect to the upper-triangular
free elements of \code{U = chol(solve(omega))} (\code{omega^-1 = t(U) \%*\% U});
its strict lower triangle is exactly 0 since those entries address no
free parameter of \code{U}. \code{NULL} if \code{omega} is not positive definite.
}
\description{
\code{rxPriorLogDensity()}'s \code{gradOmega} is always on the raw omega scale --
the prior itself is defined there, for every method (see this file's
header comment). A caller whose optimizer varies
\code{chol(Omega^-1)} internally instead (FOCEI's \code{op_focei.cholOmegaInv},
\code{nlmixr2est/src/inner.cpp}) still needs that gradient chain-ruled into its
own parameterization; a method that estimates Omega directly (SAEM) uses
\code{gradOmega} as-is and does not need this. This is a thin shim over the
pure C++ \code{rxPriorOmegaToCholOmegaInvGrad()} (\code{inst/include/rxode2prior.h}),
exposed the same way for a downstream package's own C++ objective.
}
\examples{

\donttest{
omega <- lotri::lotri(eta.ka + eta.cl ~ c(0.1, 0.01, 0.1))
gradOmega <- matrix(c(1, 0.2, 0.2, -0.5), 2, 2)
rxPriorOmegaToCholOmegaInvGrad(omega, gradOmega)
}
}
\seealso{
Other Assertions:
\code{\link[=assertCompartmentExists]{assertCompartmentExists()}},
\code{\link[=assertCompartmentName]{assertCompartmentName()}},
\code{\link[=assertCompartmentNew]{assertCompartmentNew()}},
\code{\link[=assertRxUi]{assertRxUi()}},
\code{\link[=assertVariableExists]{assertVariableExists()}},
\code{\link[=assertVariableNew]{assertVariableNew()}},
\code{\link[=rxPriorBuildSpec]{rxPriorBuildSpec()}},
\code{\link[=rxPriorLogDensity]{rxPriorLogDensity()}},
\code{\link[=rxUiPriors]{rxUiPriors()}},
\code{\link[=testIniDf]{testIniDf()}},
\code{\link[=testRxUnbounded]{testRxUnbounded()}}
}
\author{
Matthew L. Fidler
}
\concept{Assertions}
