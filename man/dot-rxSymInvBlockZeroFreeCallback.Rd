% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxSymInv.R
\name{.rxSymInvBlockZeroFreeCallback}
\alias{.rxSymInvBlockZeroFreeCallback}
\title{Register how a non-block Omega zero is parameterized}
\usage{
.rxSymInvBlockZeroFreeCallback(callback = NULL)
}
\arguments{
\item{callback}{function of no arguments returning a single logical, or
\code{NULL} to unregister.}
}
\value{
The previously registered callback, invisibly.
}
\description{
An off-diagonal zero that does not split Omega into contiguous blocks is
not a zero of \code{chol(Omega^-1)}, so it can be a free parameter (#1365).
\code{rxSymInvCholCreate()} only does that when a registered callback says so;
with no callback it errors as it always has.  This is a compatibility hook
for nlmixr2est, not a user setting: the released nlmixr2est infers its own
omega parameter positions from the zero pattern of the matrix it passes in,
and accepting that matrix rather than its filled replacement makes
\code{est="vae"} stop with a position-count mismatch.
}
\keyword{internal}
