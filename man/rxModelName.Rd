% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui-modelName.R
\name{rxModelName}
\alias{rxModelName}
\alias{rxModelName.default}
\title{Name a model from the function that created it}
\usage{
rxModelName(x, ...)

\method{rxModelName}{default}(x, ...)
}
\arguments{
\item{x}{the call that creates the model.  Its class is the name of the
called function (without any \verb{pkg::} qualifier) followed by
\code{"rxModelNameCall"}.}

\item{...}{the arguments of that call, unevaluated.}
}
\value{
single character string naming the model, or \code{NULL} when the model
cannot be named this way
}
\description{
When a model comes from a call, like
\code{rxode2(nlmixr2lib::readModelDb("PK_1cmt"))}, the text of the call is a poor
model name; the function that produced the model knows a better one.  This
is dispatched on the name of the called function, so a
\code{rxModelName.readModelDb()} method names every model \code{readModelDb()}
produces.  Without a method the call is named by its own (deparsed) text.
}
\details{
A method is given the call as \code{x} and the call's arguments in \code{...}, matched
to the argument names of the function being called whenever that function
can be found.  The arguments are unevaluated: one a method does not look at
is never evaluated, and one it does look at is evaluated again, in the frame
the call came from, so a method should only read arguments that are cheap to
evaluate.  A method that cannot name the model should return \code{NULL};
anything that is not a single non-empty string is ignored the same way.
}
\examples{

# a function that creates models from a database of them:
readMyModelDb <- function(name) {
  # ...look `name` up and return the model function...
  function() {
    ini({a <- 1})
    model({b <- a})
  }
}

# names the model for the database entry it came from
rxModelName.readMyModelDb <- function(x, ...) {
  list(...)$name
}

registerS3method("rxModelName", "readMyModelDb", rxModelName.readMyModelDb)

rxode2(readMyModelDb("one.cmt"))$modelName

}
\seealso{
Other Model names:
\code{\link[=rxModelNameFromExpr]{rxModelNameFromExpr()}},
\code{\link[=rxModelNameLhs]{rxModelNameLhs()}}
}
\author{
Matthew L. Fidler
}
\concept{Model names}
