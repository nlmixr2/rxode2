% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splitBolus.R
\name{splitBolusInfusion-directive}
\alias{splitBolusInfusion-directive}
\alias{splitBolusInfusion}
\alias{splitBolusInfusion()}
\title{Split bolus doses into bolus and infusion paths (model directive)}
\description{
\code{splitBolusInfusion()} is a model-only directive that rewrites doses
aimed at \code{cmt} into parallel doses to the target compartments,
regardless of whether the dose record is a bolus or an infusion.
Each target compartment receives the full original amount; use \code{f()}
to scale the split.

A plain bolus record targeting a compartment that declares a modeled
\code{dur()} or \code{rate()} property is promoted to a modeled infusion
start/stop pair for that compartment, so a single bolus dose record
can feed both a bolus path and an infusion path (Monolix-style
double absorption with mixed first- and zero-order routes).
Steady-state bolus records are not promoted; they are copied as
boluses with a warning when a target declares \code{dur()}/\code{rate()}.

Infusion records (data \code{RATE}/\code{DUR} or modeled \code{rate()}/\code{dur()}) are
split preserving their type, exactly like \code{\link[=splitInfusion]{splitInfusion()}}.

This rewrite applies at \code{\link[=etTrans]{etTrans()}} translation time only; doses
pushed while solving with \code{\link[=evid_]{evid_()}} are not split. Only one
splitting directive (\code{splitBolus()}, \code{splitInfusion()},
\code{splitInfusionBolus()} or \code{splitBolusInfusion()}) is allowed per
model.

There is no R function named \code{splitBolusInfusion()} exported by
rxode2; the directive is parsed from the model text directly, as
shown below.
}
\section{Usage inside a model}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{model(\{
  splitBolusInfusion(depot, depot2, central)
  dur(central) <- tk0
  f(depot2) <- 1 - f1
  f(central) <- f1
  d/dt(depot2) <- -ka2 * depot2
  d/dt(central) <- ka2 * depot2 - cl / v * central
  ...
\})
}\if{html}{\out{</div>}}

A bolus dose recorded against \code{depot} is split so \code{depot2} receives
the bolus (first-order input scaled by \code{1 - f1}) and \code{central}
receives a modeled-duration infusion (zero-order input over \code{tk0},
scaled by \code{f1}).
}

