% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etaDist.R
\name{.rxEtaDistAnchors}
\alias{.rxEtaDistAnchors}
\title{Role anchors for a declared distribution's arguments}
\usage{
.rxEtaDistAnchors(txt, eta, latent = NULL)
}
\arguments{
\item{txt}{canonical \code{etaDist} text for one declaration}

\item{eta}{the declared random effect's name}

\item{latent}{the latent name, so the collapse above is detected the same
way \code{.rxEtaDistQuantile()} detects it}
}
\value{
named character: names are the family's \code{parNames}, values the
anchor variable names; plus attribute \code{"lines"} with the assignments
}
\description{
Hoists each family argument onto its own named model line,
\verb{rxEdA.<eta>.<role>}, where \code{role} is lotri's role for that argument
(\code{shape}/\code{rate} for a gamma, \code{location}/\code{scale} for a normal).  The role
rather than the argument name, so the same group key means the same thing
across families.
}
\details{
The point is that the covariate problem becomes an ordinary one: a covariate
on a declaration's rate is a term added to one named line, which every
downstream consumer already handles, instead of a substitution inside a
quantile call.

Only arguments the family's quantile template actually uses get an anchor --
a normal-based family collapses to the latent and never references some of
its arguments, and an unused model line is dead weight.
}
\author{
Matthew L. Fidler
}
\keyword{internal}
