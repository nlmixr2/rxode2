% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{phiU}
\alias{phiU}
\alias{ibeta}
\alias{ibetaDer}
\alias{ibetaInv}
\alias{ibetaDera}
\alias{ibetaDerb}
\alias{gammapDera}
\alias{studentTDen}
\alias{studentTCdf}
\alias{studentTCdfDnu}
\alias{studentTInv}
\title{Inverse CDFs for declared non-normal random effects}
\usage{
phiU(q)

ibeta(a, b, x)

ibetaDer(a, b, x)

ibetaInv(a, b, p)

ibetaDera(a, b, x)

ibetaDerb(a, b, x)

gammapDera(a, x)

studentTDen(x, nu)

studentTCdf(x, nu)

studentTCdfDnu(x, nu)

studentTInv(p, nu)
}
\arguments{
\item{q}{normal deviate (\code{phiU()})}

\item{a}{first shape parameter}

\item{b}{second shape parameter}

\item{x}{value at which the CDF (or its derivative) is taken}

\item{p}{probability}

\item{nu}{degrees of freedom}
}
\value{
numeric vector, recycled to the longest argument
}
\description{
These are the pieces a declared non-normal random effect is built
from (see the \code{dist()} line of an \code{ini({})} block).  The latent random
effect stays standard normal, \code{phiU()} maps it to a uniform, and the
family's inverse CDF maps that uniform to the random effect.
}
\details{
\code{phiU()} is \code{phi()} bounded away from 0 and 1 by 1e-15.  \code{phi()}
saturates to exactly 0 or 1 in double precision around \verb{|q| = 8.3},
where an inverse CDF would return an infinity.

\code{ibeta()}, \code{ibetaDer()} and \code{ibetaInv()} are the regularized
incomplete beta function, its derivative in \code{x} and its inverse -- ie
\code{pbeta()}, \code{dbeta()} and \code{qbeta()}.  \code{studentTCdf()}, \code{studentTDen()}
and \code{studentTInv()} are the Student t CDF, density and quantile,
written on the incomplete beta so the quantile is exactly the
inverse of the CDF at the same tolerance.

\code{gammapDera()}, \code{ibetaDera()}, \code{ibetaDerb()} and \code{studentTCdfDnu()}
are the derivatives of those CDFs with respect to their SHAPE
parameters.  None has an elementary closed form, so they are central
differences with one Richardson extrapolation.  They exist so that
rxode2's derivative table is complete for these functions: without
them a model using an inverse CDF silently degrades to a one sided
finite difference, which is precisely wrong in the tails.
}
\examples{

phiU(c(-9, 0, 9))

ibetaInv(2, 3, 0.5) ## == qbeta(0.5, 2, 3)

studentTInv(0.975, 6) ## == qt(0.975, 6)

}
\author{
Matthew L. Fidler
}
