% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxsolve.R
\name{rxSetActiveParLoader}
\alias{rxSetActiveParLoader}
\alias{rxClearActiveParLoader}
\title{Activate a registered parameter loader for solves}
\usage{
rxSetActiveParLoader(name)

rxClearActiveParLoader()
}
\arguments{
\item{name}{Loader name, \code{"<package>:<function>"}, as registered from C with
\code{rxRegisterParLoaderNamed()}.}
}
\value{
Invisibly \code{NULL}; called for the side effect.
}
\description{
\code{rxParLoader()} flags the loader a MODEL owns, which \code{rxSolve()} applies for
that model's solve.  These activate a loader directly, for solves that do not
go through \code{rxSolve.rxUi()} -- an estimation method's internal solves, for
example, where the model being solved is assembled by the method rather than
handed to it.
}
\details{
Only the registered loader whose name is active runs, so a package can inject
its own parameters without touching an unrelated model's \code{par_ptr}.  Activate
for as short a span as possible and clear afterwards (\code{on.exit()}), because
the flag is global to the session, not attached to a model.
}
\seealso{
\code{\link[=rxParLoader]{rxParLoader()}}, \code{\link[=rxForcedPars]{rxForcedPars()}}
}
\author{
Matthew L. Fidler
}
