% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxLinCmt.R
\name{linCmtMicro}
\alias{linCmtMicro}
\title{Get the micro-constant parameterization of a linCmt() model}
\usage{
linCmtMicro(ui)
}
\arguments{
\item{ui}{rxUi-like model object}
}
\value{
A list with one element per \code{linCmt()} call in the model.
Each element is a list with \code{ncmt} (number of compartments),
\code{oral0} (1 when there is a depot compartment, 0 otherwise), and
the expressions \code{ka}, \code{v}, \code{k}, \code{k12}, \code{k21}, \code{k13} and \code{k31}.
\code{ka} is 0 for a model without a depot, and the transfer rates a
model does not have (like \code{k13} and \code{k31} for 2 compartments) are
\code{NULL}. A model without \code{linCmt()} returns an empty list.
}
\description{
This extracts the solved linear compartment model(s) from a model
and returns them as micro-constants (\code{k}, \code{k12}, \code{k21}, \code{k13},
\code{k31}), the central volume \code{v} and the absorption rate \code{ka}, each
as an R expression in terms of the model variables. This is useful
for translating a \code{linCmt()} model to software that has its own
closed-form linear compartment solutions (like NONMEM's \code{ADVAN1-4},
\code{ADVAN11-12} or Monolix's \code{pkmodel()}).
}
\examples{

oneCmt <- function() {
  ini({
    tka <- 0.45
    tcl <- log(2.7)
    tv <- 3.45
    add.sd <- 0.7
  })
  model({
    ka <- exp(tka)
    cl <- exp(tcl)
    v <- exp(tv)
    cp <- linCmt()
    cp ~ add(add.sd)
  })
}

linCmtMicro(oneCmt)

}
\author{
Matthew L. Fidler
}
