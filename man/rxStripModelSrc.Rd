% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxode2.R
\name{rxStripModelSrc}
\alias{rxStripModelSrc}
\title{Drop the srcrefs that pin a whole R session onto a compiled model}
\usage{
rxStripModelSrc(mod)
}
\arguments{
\item{mod}{compiled rxode2 model}
}
\value{
\code{mod}, modified in place
}
\description{
A compiled model is kept in \code{.rxModels} for the life of the session, so
anything it retains is retained forever.  \code{\link[utils:removeSource]{utils::removeSource()}} is already
swept over the model environment and its \code{cmpMgr}, but two closures escape
that sweep -- \code{.f} in \code{assignPtr}'s environment and \code{.badBuild} in the list
\code{.rxDll$.call} -- and each holds an independent reference to a srcref whose
\code{srcfilealias} captures the session, so both must be stripped.  Called
internally by \code{\link[=rxode2]{rxode2()}}; exported for downstream packages that build models
themselves.
}
\examples{
\donttest{
mod <- rxode2({
  d/dt(intestine) <- -a * intestine
  d/dt(blood) <- a * intestine - b * blood
})
rxStripModelSrc(mod)
}
}
\keyword{internal}
