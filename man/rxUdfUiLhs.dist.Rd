% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etaDist.R
\name{rxUdfUiLhs.dist}
\alias{rxUdfUiLhs.dist}
\title{Declare a non-Gaussian random effect distribution in the model block}
\usage{
\method{rxUdfUiLhs}{dist}(fun, rhs)
}
\arguments{
\item{fun}{the \verb{dist(<eta>)} call}

\item{rhs}{the declared distribution, eg \code{dgamma(shape=a, rate=b)}}
}
\value{
\code{rxUdfUiLhs()} list: the modified \code{iniDf} and the inverse-CDF line
that replaces the declaration
}
\description{
The \code{model({})} form of \code{ini({})}'s \code{dist()} line:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{dist(eta.cl) ~ dgamma(shape = 1/exp(lclrv), rate = 1/(exp(lclrv)*aCl))
}\if{html}{\out{</div>}}

Reached through \code{\link[=rxUdfUiLhs]{rxUdfUiLhs()}}, a user-function dispatch on the LEFT of a
model line -- \code{dist(eta.cl)} is not rxode2 grammar and \code{~} is already
overloaded, so the UI claims the whole line before rxode2 ever sees it.

The point of the model-block form is that a distribution parameter can be
any expression the model has already computed, including one built from
covariates:

\if{html}{\out{<div class="sourceCode">}}\preformatted{aCl <- exp(lclm + bWT*(WT - 70))
dist(eta.cl) ~ dgamma(shape = 1/exp(lclrv), rate = 1/(exp(lclrv)*aCl))
}\if{html}{\out{</div>}}

The \code{ini({})} form cannot express that -- it is parsed before the model
block exists, so its arguments can only name population parameters.  This
one emits the inverse-CDF line IN PLACE, at the declaration, so everything
above it is in scope.

What it leaves for \code{\link[=rxEtaDistExpand]{rxEtaDistExpand()}} is the part that needs the whole
picture rather than one line: the latent normals, the Gaussian copula that
correlates them, and the \verb{rxCor.*} thetas.  Those are prepended, so the
\verb{rxN.*} this line reads are defined above it.
}
\author{
Matthew L. Fidler
}
