% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelExtract.R
\name{modelExtract}
\alias{modelExtract}
\alias{modelExtract.function}
\title{Extract model lines from a rxui model}
\usage{
modelExtract(
  x,
  ...,
  expression = FALSE,
  endpoint = FALSE,
  envir = parent.frame()
)

\method{modelExtract}{`function`}(
  x,
  ...,
  expression = FALSE,
  endpoint = FALSE,
  envir = parent.frame()
)
}
\arguments{
\item{x}{model to extract lines from}

\item{...}{variables to extract}

\item{expression}{return expressions (if \code{TRUE}) or strings (if
\code{FALSE})}

\item{endpoint}{include endpoint}

\item{envir}{Environment for evaluating}
}
\value{
expressions or strings of extracted lines
}
\description{
Extract model lines from a rxui model
}
\examples{

  one.compartment <- function() {
   ini({
     tka <- 0.45 # Log Ka
     tcl <- 1 # Log Cl
     tv <- 3.45    # Log V
     eta.ka ~ 0.6
     eta.cl ~ 0.3
     eta.v ~ 0.1
     add.sd <- 0.7
   })
   model({
     ka <- exp(tka + eta.ka)
     cl <- exp(tcl + eta.cl)
     v <- exp(tv + eta.v)
     d/dt(depot) = -ka * depot
     d/dt(center) = ka * depot - cl / v * center
     cp = center / v
     cp ~ add(add.sd)
   })
 }

 f <- one.compartment()

 modelExtract(f, cp)

 modelExtract(one.compartment, d/dt(depot))

}
\author{
Matthew L. Fidler
}
